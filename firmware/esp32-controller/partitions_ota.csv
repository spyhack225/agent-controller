# Dual-app OTA layout for the 8 MB ESP32-S3 controller.
#
# Rollback requires two app slots plus the otadata partition: the bootloader flashes into the
# inactive slot, boots it PENDING_VERIFY, and returns to the previous slot on the next reset unless
# the firmware confirms itself (see confirmFirmwareIfPendingVerify in src/main.cpp).
#
# A single-app layout cannot roll back at all, which is why ENABLE_OTA_APPLY defaults to 0 without
# this table.
#
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x5000
otadata,    data, ota,      0xe000,   0x2000
app0,       app,  ota_0,    0x10000,  0x330000
app1,       app,  ota_1,    0x340000, 0x330000
spiffs,     data, spiffs,   0x670000, 0x180000
coredump,   data, coredump, 0x7F0000, 0x10000
