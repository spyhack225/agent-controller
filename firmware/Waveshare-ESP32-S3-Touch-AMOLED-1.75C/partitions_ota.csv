# Dual-app OTA layout for the 16 MB ESP32-S3-Touch-AMOLED-1.75C.
#
# Same contract as the CrowPanel 8 MB table, scaled to 16 MB: two app slots plus otadata, so the
# bootloader can flash the inactive slot, boot it PENDING_VERIFY, and fall back on the next reset
# unless the firmware confirms itself. A single-app layout cannot roll back at all.
#
# App slots are 5 MB each rather than the CrowPanel's 3.25 MB. This board carries an LVGL/AMOLED
# UI and an audio stack, both of which are far larger than a 122x250 e-ink menu.
#
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x5000
otadata,    data, ota,      0xe000,   0x2000
app0,       app,  ota_0,    0x10000,  0x500000
app1,       app,  ota_1,    0x510000, 0x500000
spiffs,     data, spiffs,   0xA10000, 0x5E0000
coredump,   data, coredump, 0xFF0000, 0x10000
